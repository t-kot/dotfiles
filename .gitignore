### Ignore plugin caches and local state
.nvimlog
/.DS_Store
*.swp
*.swo

### Local-only artifacts (not tracked)
*.itermexport
*.pdf

### Claude Code: machine-local (may hold private repo names / org paths)
# Template: .claude-global/settings.example.json (install.sh copies it on first run)
.claude-global/settings.json
.claude-global/skills/
